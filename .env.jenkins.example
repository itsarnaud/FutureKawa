# Local Jenkins stack (docker-compose.jenkins.yml) — copy to .env.jenkins and
# change the password before running ./scripts/start-jenkins.sh.
JENKINS_ADMIN_USER=admin
JENKINS_ADMIN_PASSWORD=change-me-locally
