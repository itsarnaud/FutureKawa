version: '3.8'

# Template de base pour éviter la duplication
x-mosquitto-template: &mosquitto-base
  image: eclipse-mosquitto:latest
  restart: unless-stopped
  networks:
    - futurekawa-network

services:
  mosquitto-bresil:
    <<: *mosquitto-base
    container_name: mosquitto-bresil
    ports:
      - "1883:1883"
    volumes:
      - ./config/mosquitto.conf:/mosquitto/config/mosquitto.conf:ro
      - ./data/bresil:/mosquitto/data

  mosquitto-equateur:
    <<: *mosquitto-base
    container_name: mosquitto-equateur
    ports:
      - "1884:1883"
    volumes:
      - ./config/mosquitto.conf:/mosquitto/config/mosquitto.conf:ro
      - ./data/equateur:/mosquitto/data

  mosquitto-colombie:
    <<: *mosquitto-base
    container_name: mosquitto-colombie
    ports:
      - "1885:1883"
    volumes:
      - ./config/mosquitto.conf:/mosquitto/config/mosquitto.conf:ro
      - ./data/colombie:/mosquitto/data

networks:
  futurekawa-network:
    driver: bridge